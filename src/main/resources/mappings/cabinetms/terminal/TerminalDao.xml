<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cabinetms.terminal.dao.TerminalDao">
    
	<sql id="terminalColumns">
		a.id AS "id",
		a.no AS "no",
		a.name AS "name",
		a.ip AS "ip",
		a.program_tactic_id AS "programTacticId",
		a.notice_tactic_id AS "noticeTacticId",
		a.unit_id AS "unitId",
		a.unit_name AS "unitName",
		a.create_by AS "createBy.id",
		a.create_date AS "createDate",
		a.update_by AS "updateBy.id",
		a.update_date AS "updateDate",
		a.remarks AS "remarks",
		a.del_flag AS "delFlag"
	</sql>
	
	<sql id="terminalJoins">
	</sql>
    
	<select id="get" resultType="Terminal">
		SELECT 
			<include refid="terminalColumns"/>
		FROM cabinetms_terminal a
		<include refid="terminalJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="Terminal">
		SELECT 
			<include refid="terminalColumns"/>
		FROM cabinetms_terminal a
		<include refid="terminalJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
			<if test="no != null and no != ''">
				AND a.no LIKE 
					<if test="dbName == 'oracle'">'%'||#{no}</if>
					<if test="dbName == 'mssql'">'%'+#{no}</if>
					<if test="dbName == 'mysql'">concat('%',#{no})</if>
			</if>
			<if test="name != null and name != ''">
				AND a.name LIKE 
					<if test="dbName == 'oracle'">'%'||#{name}||'%'</if>
					<if test="dbName == 'mssql'">'%'+#{name}+'%'</if>
					<if test="dbName == 'mysql'">concat('%',#{name},'%')</if>
			</if>
			<if test="beginCreateDate != null and endCreateDate != null and beginCreateDate != '' and endCreateDate != ''">
				AND a.create_date BETWEEN #{beginCreateDate} AND #{endCreateDate}
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="Terminal">
		SELECT 
			<include refid="terminalColumns"/>
		FROM cabinetms_terminal a
		<include refid="terminalJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO cabinetms_terminal(
			id,
			no,
			name,
			ip,
			program_tactic_id,
			notice_tactic_id,
			unit_id,
			unit_name,
			create_by,
			create_date,
			update_by,
			update_date,
			remarks,
			del_flag
		) VALUES (
			#{id},
			#{no},
			#{name},
			#{ip},
			#{programTacticId},
			#{noticeTacticId},
			#{unitId},
			#{unitName},
			#{createBy.id},
			#{createDate},
			#{updateBy.id},
			#{updateDate},
			#{remarks},
			#{delFlag}
		)
	</insert>
	
	<update id="update">
		UPDATE cabinetms_terminal SET 	
			no = #{no},
			name = #{name},
			ip = #{ip},
			program_tactic_id = #{programTacticId},
			notice_tactic_id = #{noticeTacticId},
			unit_id = #{unitId},
			unit_name = #{unitName},
			update_by = #{updateBy.id},
			update_date = #{updateDate},
			remarks = #{remarks}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		UPDATE cabinetms_terminal SET 
			del_flag = #{DEL_FLAG_DELETE}
		WHERE id = #{id}
	</update>
	
</mapper>