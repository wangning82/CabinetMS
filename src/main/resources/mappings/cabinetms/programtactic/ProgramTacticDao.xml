<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cabinetms.programtactic.dao.ProgramTacticDao">
    
	<sql id="programTacticColumns">
		a.id AS "id",
		a.no AS "no",
		a.name AS "name",
		a.program_no AS "programNo",
		a.program_name AS "programName",
		a.starttime AS "starttime",
		a.endtime AS "endtime",
		a.create_by AS "createBy.id",
		a.create_date AS "createDate",
		a.update_by AS "updateBy.id",
		a.update_date AS "updateDate",
		a.remarks AS "remarks",
		a.del_flag AS "delFlag"
	</sql>
	
	<sql id="programTacticJoins">
	</sql>
    
	<select id="get" resultType="ProgramTactic">
		SELECT 
			<include refid="programTacticColumns"/>
		FROM cabinetms_program_tactic a
		<include refid="programTacticJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="ProgramTactic">
		SELECT 
			<include refid="programTacticColumns"/>
		FROM cabinetms_program_tactic a
		<include refid="programTacticJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
			<if test="name != null and name != ''">
				AND a.name LIKE 
					<if test="dbName == 'oracle'">'%'||#{name}||'%'</if>
					<if test="dbName == 'mssql'">'%'+#{name}+'%'</if>
					<if test="dbName == 'mysql'">concat('%',#{name},'%')</if>
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="ProgramTactic">
		SELECT 
			<include refid="programTacticColumns"/>
		FROM cabinetms_program_tactic a
		<include refid="programTacticJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO cabinetms_program_tactic(
			id,
			no,
			name,
			program_no,
			program_name,
			starttime,
			endtime,
			create_by,
			create_date,
			update_by,
			update_date,
			remarks,
			del_flag
		) VALUES (
			#{id},
			#{no},
			#{name},
			#{programNo},
			#{programName},
			#{starttime},
			#{endtime},
			#{createBy.id},
			#{createDate},
			#{updateBy.id},
			#{updateDate},
			#{remarks},
			#{delFlag}
		)
	</insert>
	
	<update id="update">
		UPDATE cabinetms_program_tactic SET 	
			no = #{no},
			name = #{name},
			program_no = #{programNo},
			program_name = #{programName},
			starttime = #{starttime},
			endtime = #{endtime},
			update_by = #{updateBy.id},
			update_date = #{updateDate},
			remarks = #{remarks}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		UPDATE cabinetms_program_tactic SET 
			del_flag = #{DEL_FLAG_DELETE}
		WHERE id = #{id}
	</update>
	
</mapper>